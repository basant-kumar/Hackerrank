#include <iostream>
#include <vector>

using namespace std;

int main(){
    int n;
    vector<int> a[n];
    for(int i=0;i<n;++i)
        cin>>(a.push_back(i));
    cout<<a.size()<<endl;

    return 0;
}